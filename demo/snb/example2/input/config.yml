defaults:
  model_name: mace-mpa-0-medium
  outputs_root: ../results
  device: cuda
  dtype: float32
  overwrite: false
  include_vdw: true
  prepare_dft: false
workflows:
  snb:
    settings:
      fmax: 0.03
      max_steps: 300
      energy_window_eV: 0.3
      max_clusters_per_model: 4
