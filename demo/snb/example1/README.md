# SnB bulk/defect demo

This example runs ShakeNBreak from one bulk structure and one defect structure, then writes VASP-ready folders under `dft_validate`.

```bash
mlip-snb --inputs demo/snb/example1/input
```
