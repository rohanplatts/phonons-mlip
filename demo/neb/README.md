# NEB demo

This folder contains a real beta-CsPbI3 NEB input directory in `example1`.
It includes the two endpoint structures and the DFT path data used by
`mlip-neb`.

Run it with:

```bash
mlip-neb --inputs demo/neb/example1/input
```
