# NEB demo

This example runs a real NEB path for the beta-CsPbI3 neutral interstitial case using two relaxed endpoints and a path file from the training data.

```bash
mlip-neb --inputs demo/neb/example1/input
```
