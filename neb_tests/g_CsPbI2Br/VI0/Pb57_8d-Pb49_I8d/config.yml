defaults:
  model_name: mace_neutral_charge_ft_mh_09_07_26
  outputs_root: ./results_7img
  poscar_i: POSCAR_i
  poscar_f: POSCAR_f
  #dft_neb_dat: neb.dat
  device: cuda
  dtype: float32
  overwrite: true
  include_vdw: true
  relax_endpoints: false
  remap_f_i: true
workflows:
  neb:
    settings:
      # no. images includes endpoints; add 2 cf vtstt
      # maybe change how this works to better interface, would be very easy
      n_images_fallback: 7
      # settings for initial rough guess
      fmax_mlip_guess: 0.01
      steps_mlip_guess: 300
      # settings for vdW-D3 corrections
      fmax_mlip_d3: 0.01
      steps_mlip_d3: 300
      # settings for CI process
      fmax_ci: 0.01
      steps_ci: 300
